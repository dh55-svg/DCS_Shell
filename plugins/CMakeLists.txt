add_subdirectory(fieldbus_simulator)
add_subdirectory(persistence_sqlite)
# add_subdirectory(fieldbus_modbus)  # requires libmodbus
# add_subdirectory(mqtt_qt)          # requires Qt6::Mqtt (optional)
